<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SASS.Views.Authentication.SignInPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="120" />
                <RowDefinition Height="*" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <Frame Padding="0" BackgroundColor="{StaticResource SASS_Default_1}" Grid.Row="1" >
                <ScrollView >
                    <StackLayout >
                        <Entry Style="{StaticResource entryStyle}" />
                        <Entry Style="{StaticResource entryStyle}" />
                        <Entry Style="{StaticResource entryStyle}" />
                        <Entry Style="{StaticResource entryStyle}" />
                        


                    </StackLayout>
                    
                </ScrollView>
                
                
                
            </Frame>

            <Frame Padding="0" BackgroundColor="Transparent" Grid.Row="2" >
                <Button Text="Create Account" HorizontalOptions="End" ></Button> 
            </Frame>
            
            
            
        </Grid>
    </ContentPage.Content>
</ContentPage>